name: sim
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - matplotlib
      - plotly
      - streamlit
      - pyarrow
      - pydantic
      - scipy
      - scikit-learn
      - ta
      - tqdm
      - loguru
      - pytest
      - black
      - ruff
      - mypy
      - yfinance
      - fastapi
      - uvicorn
      - httpx
